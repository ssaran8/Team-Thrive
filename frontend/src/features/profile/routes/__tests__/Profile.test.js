import {render, screen} from "@testing-library/react"
import { Profile } from "../Profile"

describe("Test Profile Page", () =>
    test("dummy Profile test", async () => {
        // TODO: finish Profile page before adding tests
        expect(true).toBeTruthy();
    })
)